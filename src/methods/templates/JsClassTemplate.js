export default class TestClass {

  constructor(testArgs) {
        
  this.State = {
    testState: "state reached!"
  };

      console.log(testArgs);

  } 
}

// const testArgs = {}
// new TestClass(testArgs)
